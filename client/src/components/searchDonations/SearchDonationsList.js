import React from "react";

const SearchDonationsList = ({ children }) => {
  return (
  <div class="columns">
    <div class="column is-one-fifth">
        {children}
      </div>
  </div>

  )

};

export default SearchDonationsList;